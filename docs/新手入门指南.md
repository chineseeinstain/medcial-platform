# 新手入门指南 - Java全栈开发快速理解

## 🎯 本文档目标

帮助Java开发新手快速理解：
1. 项目整体架构
2. 各技术的作用和关系
3. 代码如何配合工作
4. 如何开始开发

## 📚 第一部分：理解项目结构

### 1.1 项目目录结构

```
MDEICAL-PLATFORM/
├── backend/                    # 后端代码（Java）
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/medical/
│   │   │   │       ├── controller/    # API接口层（接收HTTP请求）
│   │   │   │       ├── service/       # 业务逻辑层（处理业务）
│   │   │   │       ├── mapper/        # 数据访问层（操作数据库）
│   │   │   │       ├── entity/        # 实体类（对应数据库表）
│   │   │   │       ├── dto/           # 数据传输对象（API参数）
│   │   │   │       └── config/        # 配置类（Redis、Security等）
│   │   │   └── resources/
│   │   │       ├── mapper/            # MyBatis SQL映射文件
│   │   │       └── application.yml    # 配置文件
│   │   └── test/                      # 测试代码
│   └── pom.xml                        # Maven依赖管理
│
├── frontend/                   # 前端代码（Vue 3）
│   ├── src/
│   │   ├── views/              # 页面组件
│   │   ├── components/         # 可复用组件
│   │   ├── api/                # API请求封装
│   │   ├── router/             # 路由配置
│   │   └── utils/              # 工具函数
│   └── package.json            # npm依赖管理
│
├── python-analysis/            # Python分析脚本
│   ├── drg_analysis.py        # DRG病组分析
│   └── resource_analysis.py   # 资源利用率分析
│
└── database/                   # 数据库脚本
    ├── init.sql                # 初始化脚本
    └── schema.sql              # 表结构定义
```

### 1.2 三层架构理解（后端）

```
┌─────────────────────────────────────┐
│  Controller层（控制器）              │
│  作用：接收HTTP请求，返回JSON响应    │
│  位置：backend/.../controller/       │
│  示例：PatientController.java        │
└──────────────┬──────────────────────┘
               │ 调用
               ↓
┌─────────────────────────────────────┐
│  Service层（业务逻辑）               │
│  作用：处理业务逻辑，调用Mapper       │
│  位置：backend/.../service/          │
│  示例：PatientService.java           │
└──────────────┬──────────────────────┘
               │ 调用
               ↓
┌─────────────────────────────────────┐
│  Mapper层（数据访问）                │
│  作用：执行SQL，操作数据库            │
│  位置：backend/.../mapper/           │
│  示例：PatientMapper.java            │
└──────────────┬──────────────────────┘
               │ SQL查询
               ↓
            MySQL数据库
```

## 🔧 第二部分：各技术工具详解

### 2.1 Spring Boot - 后端框架

**作用**：快速搭建Java Web应用

**核心概念**：
- **@RestController**：标识这是一个API控制器
- **@Service**：标识业务逻辑类
- **@Autowired**：自动注入依赖（依赖注入）
- **@RequestMapping**：定义API路径

**示例理解**：
```java
@RestController  // 告诉Spring：这是一个API控制器
@RequestMapping("/api/patients")  // API路径：/api/patients
public class PatientController {
    
    @Autowired  // 自动注入PatientService，不需要new
    private PatientService patientService;
    
    @GetMapping("/list")  // GET请求：/api/patients/list
    public List<Patient> getPatients() {
        return patientService.getAllPatients();  // 调用Service
    }
}
```

**为什么用Spring Boot**：
- 自动配置：不需要写很多XML配置
- 内置服务器：直接运行，不需要Tomcat
- 依赖管理：Maven自动下载依赖

### 2.2 MyBatis - 数据库操作框架

**作用**：将Java对象和SQL语句映射起来

**工作方式**：
1. 定义Mapper接口（Java接口）
2. 写SQL映射文件（XML）
3. MyBatis自动执行SQL，返回Java对象

**示例理解**：
```java
// Mapper接口（Java）
public interface PatientMapper {
    List<Patient> selectAll();  // 方法名对应SQL
}
```

```xml
<!-- SQL映射文件（XML） -->
<mapper namespace="com.medical.mapper.PatientMapper">
    <select id="selectAll" resultType="Patient">
        SELECT * FROM patient_visit
    </select>
</mapper>
```

**为什么用MyBatis**：
- 灵活：可以写复杂SQL
- 性能：SQL可控，可以优化
- 简单：比JPA更直观

### 2.3 MySQL - 数据库

**作用**：存储数据（患者、科室、医保等）

**理解**：
- 数据库 = 仓库
- 表 = 货架
- 行 = 一条记录
- 列 = 字段

**示例表结构**：
```sql
-- 患者诊疗表
CREATE TABLE patient_visit (
    id BIGINT PRIMARY KEY,           -- 主键
    patient_id VARCHAR(50),          -- 患者ID（脱敏后）
    visit_date DATE,                 -- 就诊日期
    department VARCHAR(50),          -- 科室
    diagnosis VARCHAR(200),          -- 诊断
    cost DECIMAL(10,2)               -- 费用
);
```

### 2.4 Redis - 缓存

**作用**：临时存储热点数据，提升速度

**理解**：
- 内存数据库，速度极快
- 存储键值对（key-value）
- 设置过期时间，自动清除

**使用场景**：
```java
// 查询前先查缓存
String cacheKey = "departments:list";
List<Department> departments = redis.get(cacheKey);

if (departments == null) {
    // 缓存没有，查数据库
    departments = departmentMapper.selectAll();
    // 存入缓存，5分钟过期
    redis.set(cacheKey, departments, 300);
}

return departments;
```

**为什么用Redis**：
- 速度快：内存操作，比MySQL快100倍
- 减少数据库压力：热点数据不重复查询
- 提升用户体验：API响应更快

### 2.5 Vue 3 - 前端框架

**作用**：构建用户界面

**核心概念**：
- **组件**：可复用的UI块
- **响应式**：数据变化，界面自动更新
- **路由**：页面跳转

**示例理解**：
```vue
<template>
  <!-- HTML模板 -->
  <div>
    <h1>患者列表</h1>
    <table>
      <tr v-for="patient in patients" :key="patient.id">
        <td>{{ patient.name }}</td>
      </tr>
    </table>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { getPatients } from '@/api/patient'

// 响应式数据
const patients = ref([])

// 页面加载时获取数据
onMounted(async () => {
  patients.value = await getPatients()  // 调用API
})
</script>
```

### 2.6 ECharts - 图表库

**作用**：数据可视化，画图表

**使用方式**：
```javascript
// 1. 准备数据
const data = {
  xAxis: ['1月', '2月', '3月'],
  series: [120, 200, 150]
}

// 2. 配置图表
const option = {
  xAxis: { data: data.xAxis },
  yAxis: {},
  series: [{ data: data.series }]
}

// 3. 渲染图表
chart.setOption(option)
```

### 2.7 Python脚本 - 数据分析

**作用**：处理复杂数据分析任务

**为什么用Python**：
- Pandas处理表格数据方便
- NumPy数值计算强大
- 适合数据分析和建模

**调用方式（Java调用Python）**：
```java
// Java代码中调用Python脚本
ProcessBuilder pb = new ProcessBuilder(
    "python", 
    "python-analysis/drg_analysis.py",
    inputDataPath
);
Process process = pb.start();
// 读取Python输出结果
```

## 🔄 第三部分：数据流转理解

### 3.1 完整请求流程示例

**场景**：用户在前端查看"门诊量趋势"

#### 步骤1：前端发送请求
```javascript
// frontend/src/api/statistics.js
export function getOutpatientTrend() {
  return axios.get('/api/statistics/outpatient-trend')
}
```

#### 步骤2：后端Controller接收
```java
// backend/.../controller/StatisticsController.java
@RestController
@RequestMapping("/api/statistics")
public class StatisticsController {
    
    @Autowired
    private StatisticsService statisticsService;
    
    @GetMapping("/outpatient-trend")
    public Result<List<TrendData>> getOutpatientTrend() {
        List<TrendData> data = statisticsService.getOutpatientTrend();
        return Result.success(data);
    }
}
```

#### 步骤3：Service处理业务逻辑
```java
// backend/.../service/StatisticsService.java
@Service
public class StatisticsService {
    
    @Autowired
    private StatisticsMapper statisticsMapper;
    
    @Autowired
    private RedisTemplate<String, Object> redis;
    
    public List<TrendData> getOutpatientTrend() {
        // 1. 先查缓存
        String cacheKey = "statistics:outpatient-trend";
        List<TrendData> cached = (List<TrendData>) redis.opsForValue().get(cacheKey);
        if (cached != null) {
            return cached;  // 有缓存直接返回
        }
        
        // 2. 查数据库
        List<TrendData> data = statisticsMapper.selectOutpatientTrend();
        
        // 3. 存入缓存（5分钟）
        redis.opsForValue().set(cacheKey, data, 5, TimeUnit.MINUTES);
        
        return data;
    }
}
```

#### 步骤4：Mapper查询数据库
```java
// backend/.../mapper/StatisticsMapper.java
public interface StatisticsMapper {
    List<TrendData> selectOutpatientTrend();
}
```

```xml
<!-- backend/.../resources/mapper/StatisticsMapper.xml -->
<select id="selectOutpatientTrend" resultType="TrendData">
    SELECT 
        DATE(visit_date) as date,
        COUNT(*) as count
    FROM patient_visit
    WHERE visit_date >= DATE_SUB(NOW(), INTERVAL 30 DAY)
    GROUP BY DATE(visit_date)
    ORDER BY date
</select>
```

#### 步骤5：数据返回前端
```javascript
// 前端接收数据
const response = await getOutpatientTrend()
const chartData = response.data

// ECharts渲染图表
chart.setOption({
  xAxis: { data: chartData.map(d => d.date) },
  series: [{ data: chartData.map(d => d.count) }]
})
```

### 3.2 数据分析流程示例

**场景**：生成DRG病组分析报告

```
1. 前端请求：POST /api/analysis/drg-report
   ↓
2. Controller接收请求
   ↓
3. Service从MySQL读取患者数据
   ↓
4. Service调用Python脚本
   ProcessBuilder执行：python drg_analysis.py input.json
   ↓
5. Python脚本处理：
   - 读取JSON数据
   - Pandas分析
   - 生成DRG分组
   - 输出结果JSON
   ↓
6. Service读取Python输出
   ↓
7. 结果写入MySQL或返回前端
   ↓
8. 前端展示分析报告
```

## 🚀 第四部分：如何开始开发

### 4.1 开发环境搭建

1. **安装JDK 1.8+**
   - 下载：Oracle JDK 或 OpenJDK
   - 配置JAVA_HOME环境变量

2. **安装Maven**
   - 下载Maven
   - 配置MAVEN_HOME

3. **安装MySQL**
   - 下载MySQL 8.0
   - 创建数据库：`CREATE DATABASE medical_platform;`

4. **安装Redis**
   - 下载Redis
   - 启动：`redis-server`

5. **安装Node.js**
   - 下载Node.js 16+
   - 安装npm

6. **安装Python 3.8+**
   - 安装Pandas、NumPy：`pip install pandas numpy`

### 4.2 项目启动步骤

#### 启动后端
```bash
cd backend
mvn clean install      # 下载依赖，编译
mvn spring-boot:run    # 启动服务（端口8080）
```

#### 启动前端
```bash
cd frontend
npm install            # 安装依赖
npm run dev            # 启动开发服务器（端口3000）
```

#### 初始化数据库
```bash
mysql -u root -p medical_platform < database/init.sql
```

### 4.3 开发第一个功能

**任务**：实现"查询患者列表"功能

#### 步骤1：创建数据库表
```sql
CREATE TABLE patient_visit (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    patient_id VARCHAR(50),
    visit_date DATE,
    department VARCHAR(50)
);
```

#### 步骤2：创建实体类
```java
// backend/.../entity/Patient.java
public class Patient {
    private Long id;
    private String patientId;
    private Date visitDate;
    private String department;
    // getter/setter...
}
```

#### 步骤3：创建Mapper
```java
// backend/.../mapper/PatientMapper.java
public interface PatientMapper {
    List<Patient> selectAll();
}
```

```xml
<!-- backend/.../resources/mapper/PatientMapper.xml -->
<select id="selectAll" resultType="Patient">
    SELECT * FROM patient_visit
</select>
```

#### 步骤4：创建Service
```java
// backend/.../service/PatientService.java
@Service
public class PatientService {
    @Autowired
    private PatientMapper patientMapper;
    
    public List<Patient> getAllPatients() {
        return patientMapper.selectAll();
    }
}
```

#### 步骤5：创建Controller
```java
// backend/.../controller/PatientController.java
@RestController
@RequestMapping("/api/patients")
public class PatientController {
    @Autowired
    private PatientService patientService;
    
    @GetMapping("/list")
    public Result<List<Patient>> getPatients() {
        return Result.success(patientService.getAllPatients());
    }
}
```

#### 步骤6：前端调用
```javascript
// frontend/src/api/patient.js
export function getPatients() {
  return axios.get('/api/patients/list')
}
```

```vue
<!-- frontend/src/views/PatientList.vue -->
<template>
  <div>
    <table>
      <tr v-for="p in patients" :key="p.id">
        <td>{{ p.patientId }}</td>
        <td>{{ p.department }}</td>
      </tr>
    </table>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { getPatients } from '@/api/patient'

const patients = ref([])
onMounted(async () => {
  patients.value = (await getPatients()).data
})
</script>
```

## 💡 第五部分：常见问题理解

### Q1: Controller、Service、Mapper的区别？

**A**: 
- **Controller**：接收HTTP请求，像"前台接待"
- **Service**：处理业务逻辑，像"业务经理"
- **Mapper**：操作数据库，像"仓库管理员"

### Q2: 为什么要分层？

**A**: 
- 职责清晰：每层只做自己的事
- 易于维护：修改数据库不影响业务逻辑
- 易于测试：可以单独测试每一层

### Q3: Redis缓存什么时候用？

**A**: 
- 查询频繁的数据（如科室列表）
- 计算耗时的数据（如统计数据）
- 不经常变化的数据

### Q4: 前端和后端如何通信？

**A**: 
- 前端通过HTTP请求（GET/POST）调用后端API
- 后端返回JSON数据
- 前端解析JSON，渲染到页面

### Q5: Python脚本如何集成？

**A**: 
- Java通过ProcessBuilder执行Python脚本
- Python读取输入数据（JSON/CSV）
- Python处理数据，输出结果（JSON）
- Java读取输出，返回给前端

## 📖 学习路径建议

### 第1周：理解基础
1. 学习Java基础语法
2. 理解Spring Boot基本概念
3. 理解MVC架构（Model-View-Controller）

### 第2周：数据库操作
1. 学习MySQL基本SQL
2. 理解MyBatis使用方式
3. 练习CRUD操作

### 第3周：前后端交互
1. 学习RESTful API设计
2. 学习Vue 3基础
3. 练习前后端联调

### 第4周：进阶功能
1. 学习Redis缓存
2. 学习Spring Security权限
3. 学习ECharts图表

## 🎓 推荐学习资源

- **Spring Boot官方文档**：https://spring.io/projects/spring-boot
- **Vue 3官方文档**：https://vuejs.org/
- **MyBatis官方文档**：https://mybatis.org/mybatis-3/
- **ECharts官方文档**：https://echarts.apache.org/

---

**记住**：编程是实践的艺术，多写代码，多调试，多思考！

