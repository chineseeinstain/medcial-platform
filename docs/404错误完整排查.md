# 404错误完整排查指南

## 📋 路径映射验证

### 前端请求路径
```
前端代码: auth.js
  url: '/auth/login'
  baseURL: '/api' (来自 request.js)
  ──────────────────────────────
  实际请求: /api/auth/login
```

### Vite代理配置
```
vite.config.js
  proxy: '/api' -> 'http://localhost:8080'
  ──────────────────────────────
  代理后URL: http://localhost:8080/api/auth/login
```

### 后端Controller路径
```
AuthController.java
  @RequestMapping("/api/auth")
  @PostMapping("/login")
  ──────────────────────────────
  完整路径: /api/auth/login ✅ 匹配
```

**结论：路径映射是正确的！**

---

## 🔍 404错误的可能原因

### 1. 后端服务未启动 ⚠️ **最常见（90%的情况）**

**检查方法：**
```bash
# 方法1：访问健康检查接口
curl http://localhost:8080/health

# 方法2：访问根路径
curl http://localhost:8080/

# 方法3：检查端口占用
netstat -ano | findstr :8080
```

**解决方法：**
```bash
# 进入后端目录
cd backend

# 启动后端（Maven）
mvn spring-boot:run

# 或使用IDE运行
# 打开 MedicalPlatformApplication.java -> 右键 -> Run
```

**预期结果：**
```
Started MedicalPlatformApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)
```

---

### 2. 后端启动失败

**常见原因：**

#### A. 数据库连接失败
```
错误信息：Cannot create PoolableConnectionFactory
解决：检查MySQL服务是否启动，数据库配置是否正确
```

#### B. 端口被占用
```
错误信息：Port 8080 is already in use
解决：
  netstat -ano | findstr :8080
  taskkill /PID <PID> /F
```

#### C. 依赖缺失
```
错误信息：ClassNotFoundException
解决：
  cd backend
  mvn clean install
```

#### D. Redis连接失败（如果启用了Redis）
```
解决：注释掉 application.yml 中的 Redis 配置，或启动Redis服务
```

---

### 3. Controller未被扫描

**检查点：**
- ✅ `@SpringBootApplication` 在主类上
- ✅ Controller在 `com.medical.controller` 包下
- ✅ 主类在 `com.medical` 包下（会自动扫描子包）

**验证方法：**
启动后端后，访问：
```
http://localhost:8080/swagger-ui/index.html
```
如果能看到 `/api/auth/login` 接口，说明Controller已扫描。

---

### 4. Spring Security配置问题

**当前配置：**
```java
.anyRequest().permitAll()  // 允许所有请求 ✅
```

**如果修改过SecurityConfig，确保：**
- CSRF已禁用：`.csrf().disable()`
- 所有请求允许：`.anyRequest().permitAll()`

---

### 5. Vite代理未生效

**检查方法：**
1. 确认前端服务运行在3000端口
2. 确认后端服务运行在8080端口
3. 重启前端服务：`npm run dev`

**验证代理：**
浏览器Network标签页中，请求URL应该是：
```
Request URL: http://localhost:3000/api/auth/login
```
如果显示的是 `http://localhost:8080/api/auth/login`，说明代理未生效。

---

## 🛠️ 快速诊断步骤

### 步骤1：检查后端服务状态
```bash
curl http://localhost:8080/health
```
**期望响应：**
```json
{"code":200,"message":"操作成功","data":{"status":"UP"},"timestamp":...}
```

### 步骤2：测试登录接口
```bash
curl -X POST http://localhost:8080/api/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"admin\",\"password\":\"123456\"}"
```
**期望响应：**
```json
{"code":200,"message":"登录成功","data":{"token":"...","username":"admin",...},"timestamp":...}
```

### 步骤3：检查后端日志
查看后端控制台，确认：
- ✅ 看到 "Started MedicalPlatformApplication"
- ✅ 看到 "Tomcat started on port(s): 8080"
- ✅ 没有错误堆栈

### 步骤4：检查浏览器Network
1. 打开开发者工具（F12）
2. 切换到Network标签页
3. 尝试登录
4. 查看请求详情：
   - **Request URL**: `http://localhost:3000/api/auth/login`
   - **Status Code**: 如果是404，查看实际请求的URL
   - **Response**: 查看错误详情

---

## 📝 诊断清单

请逐项检查：

- [ ] **后端服务已启动**
  - [ ] 可以访问 `http://localhost:8080/health`
  - [ ] 后端控制台显示 "Started MedicalPlatformApplication"
  - [ ] 端口8080正在监听

- [ ] **数据库连接正常**
  - [ ] MySQL服务已启动
  - [ ] 数据库 `medical_platform` 已创建
  - [ ] `application.yml` 中的数据库配置正确

- [ ] **Controller已扫描**
  - [ ] 可以访问 `http://localhost:8080/swagger-ui/index.html`
  - [ ] 能看到 `/api/auth/login` 接口

- [ ] **前端代理配置正确**
  - [ ] `vite.config.js` 中代理配置正确
  - [ ] 前端服务运行在3000端口
  - [ ] 浏览器Network中请求URL正确

- [ ] **路径映射正确**
  - [ ] 前端请求：`/api/auth/login`
  - [ ] 后端路径：`/api/auth/login`
  - [ ] 两者匹配

---

## 🎯 关键线索

### 线索1：如果curl测试也返回404
**说明：** 后端服务可能未启动，或Controller未被扫描
**行动：** 检查后端启动日志，确认Controller是否加载

### 线索2：如果curl测试成功，但前端404
**说明：** Vite代理配置问题，或前端请求URL错误
**行动：** 检查浏览器Network标签页，查看实际请求URL

### 线索3：如果后端启动失败
**说明：** 配置错误或依赖问题
**行动：** 查看后端控制台的完整错误堆栈

### 线索4：如果后端启动成功，但接口404
**说明：** Controller路径映射问题
**行动：** 访问Swagger UI，确认接口是否存在

---

## 🔧 常用命令

```bash
# 检查后端服务
curl http://localhost:8080/health

# 测试登录接口
curl -X POST http://localhost:8080/api/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"admin\",\"password\":\"123456\"}"

# 检查端口占用
netstat -ano | findstr :8080

# 结束占用8080端口的进程
taskkill /PID <PID> /F

# 重新编译后端
cd backend
mvn clean install

# 启动后端
mvn spring-boot:run
```

---

## 📞 如果问题仍未解决

请提供以下信息：

1. **后端控制台完整日志**（特别是启动时的日志）
2. **浏览器Network标签页截图**（显示请求详情）
3. **curl测试结果**（`curl http://localhost:8080/health`）
4. **后端是否成功启动**（是否看到 "Started MedicalPlatformApplication"）

